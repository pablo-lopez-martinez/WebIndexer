# WebIndexer
A multithreaded application that processes .url text files. Each thread handles reading and processing each .url file. Processing each file involves downloading the pages, parsing them, and indexing them.
